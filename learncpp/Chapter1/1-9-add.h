// Header guard, so ADD_H is a unique name and prevents a header file from being included more than once

#ifndef ADD_H
#define ADD_H

// Content of the header file, declarations go here

int add(int x, int y);

// close the header guard
#endif
